// script.js
console.log('BuilderFlow clone initialized');

// Navbar scroll effect (optional enhancement later)
window.addEventListener('scroll', () => {
    const navbar = document.querySelector('.navbar');
    // Logic to possibly shrink navbar or change opacity on scroll
});
